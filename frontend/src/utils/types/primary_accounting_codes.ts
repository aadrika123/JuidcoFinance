export type PrimaryAccCodes = {
    ulb_id: number | string;
    request_no: string;
    employee_id: number | string;
    date: string;
    group: string;
    reference_code: string;
    description: string;
}